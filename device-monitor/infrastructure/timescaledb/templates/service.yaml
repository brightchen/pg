apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-timescaledb
  labels:
    app: timescaledb
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    name: postgres
  selector:
    app: timescaledb
